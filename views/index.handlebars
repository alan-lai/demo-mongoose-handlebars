{{#if authors.length }}
    <ul class="authors">
        {{#each authors }}
            <li><a href="/author/{{lowercase this.last_name }}/{{lowercase this.first_name }}">{{ this.first_name }} {{ this.last_name }}</a></li>
        {{/each}}
    </ul>
{{else}}
    <p>No authors found.</p>
{{/if}}